

## Reanudación automática al recuperar el límite
Claude Code (v2.1.234 o posterior) puede quedarse esperando y continuar solo cuando el límite se restablece. Se activa en /config → Continue automatically at usage limit, o directo:


/config autoContinueAtUsageLimit=true