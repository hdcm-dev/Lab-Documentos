<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Único paquete de terceros del árbol: el micro-ORM que mide el escenario `materializadores` (V09).
       Dapper 2.1.86, Apache-2.0 (licencia declarada en su nuspec), https://dapperlib.dev/ -->
  <ItemGroup>
    <PackageReference Include="Dapper" Version="2.1.86" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Domain\FabricaYResultado.Domain.csproj" />
    <ProjectReference Include="..\Infrastructure\FabricaYResultado.Infrastructure.csproj" />
  </ItemGroup>

</Project>
